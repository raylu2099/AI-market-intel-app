version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
    environment:
      - MARKET_TZ=US/Pacific
    restart: unless-stopped
